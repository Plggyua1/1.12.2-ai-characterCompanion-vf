'use strict'

module.exports = {
  minecraft: {
    host: 'localhost',     // or server IP / domain
    port: 25565,
    version: '1.12.2',
    auth: 'offline'        // or 'mojang' later
  },

  statusServer: {
    enabled: true,
    port: 8787
  }
}
